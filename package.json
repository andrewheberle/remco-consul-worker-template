{
	"name": "remco-consul-worker-template",
	"private": true,
	"cloudflare": {
		"bindings": {
			"KV": {
				"decription": "The Workers KV namespace to use for storing Key/Value data"
			}
		}
	},
	"scripts": {
		"cf-typegen": "wrangler types",
		"dev": "wrangler dev",
		"schema": "chanfana",
		"test:workers": "vitest run"
	},
	"dependencies": {
		"@andrewheberle/remco-consul-worker": "^0.1.5"
	},
	"devDependencies": {
		"vitest": "^4.1.7",
		"wrangler": "^4.95.0"
	},
	"release-it": {
		"git": false,
		"npm": {
			"publish": false
		}
	}
}
