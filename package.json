{
	"name": "remco-consul-worker-template",
	"private": true,
	"cloudflare": {
		"bindings": {
			"KV": {
				"decription": "The Workers KV namespace to use for storing Key/Value data"
			}
		}
	},
	"scripts": {
		"cf-typegen": "wrangler types",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev"
	},
	"dependencies": {
		"@andrewheberle/remco-consul-worker": "^0.2.0"
	},
	"devDependencies": {
		"wrangler": "^4.95.0"
	}
}
